# Configuration reference

Radix can be configured via `radix.yml`, `RADIX_*` environment variables, and
CLI flags.

> 🚧 Documentation in progress

## Configuration file

## `radix.yml` keys

## Environment overrides (`RADIX_*`)

## Precedence

CLI flags &gt; environment variables &gt; config file &gt; defaults.
